[pytest]
DJANGO_SETTINGS_MODULE = config.settings
python_files = test_*.py
testpaths = blog/tests
pythonpath = .
addopts =
    --cov=blog
    --cov-report=html
    --cov-report=term
    --cov-report=xml
    -v
    --reuse-db
    --nomigrations